/**
 * Default SYSTEM object definitions for the platform
 *
 * These are minimal, generic object types that demonstrate the flexible schema system.
 * Domain-specific packages (CRM, Ecom, etc.) will define their own object types.
 */

export * from "./default-object-definition";

/**
 * Example of how domain packages can define their objects
 */
export * from "./example-domain-objects";
