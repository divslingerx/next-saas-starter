import type { Config } from "tailwindcss";
import baseConfig from "@charmlabs/tailwind-config/web";

export default {
  content: ["./src/**/*.{ts,tsx}"],
  presets: [baseConfig],
  theme: {
    extend: {
      // Add any package-specific customizations here
    },
  },
} satisfies Config;